# Server-only — never expose in frontend code
DEEPSEEK_API_KEY=your_key_here

# Optional: API server port (default 3001)
PORT=3001

# Set to 1 if you see "self-signed certificate in certificate chain"
# (common with VPN, antivirus, or corporate SSL inspection on your Mac)
DEEPSEEK_TLS_INSECURE=0
