# Server-only — never expose in frontend code
# Copy to .env and replace with your real key from https://platform.deepseek.com
DEEPSEEK_API_KEY=

# Optional: only if TLS certificate errors occur (mainly local dev)
DEEPSEEK_TLS_INSECURE=0
